
#debug: true

management:
  endpoints:
    web:
      exposure:
        include: "env, info, health, loggers"
        #include: "*"
  endpoint:
    env:
      show-values: "always"
    health:
      show-details: "always"

greeting:
  word: "Hallo"
  name: "${greeting.word} Dave"

droid:
  id: "B28"
  description: "This is simple droid"